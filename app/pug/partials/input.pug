mixin input(props = {})
  -const { classes, bordered, size, stadard } = props
  -
    const classnames = classNames({
      'input': stadard !== false,
      'input--bordered': bordered,
      [`input--${size}`]: size,
      [classes]: classes,
    })

  input(class=classnames)&attributes(attributes)
