mixin input(props = {})
  -const { classes, bordered, size, standard, hidden } = props
  -
    const classnames = classNames({
      'input': standard !== false,
      'input--bordered': bordered,
      'input--hidden': hidden,
      [`input--${size}`]: size,
      [classes]: classes,
    })

  input(class=classnames)&attributes(attributes)
